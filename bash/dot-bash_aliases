# cat alias to bat
if type bat &> /dev/null; then
	alias cat='bat --style=plain --paging=never'
fi

# neofetch alias to fastfetch
if type fastfetch &> /dev/null; then
	alias neofetch='fastfetch --config neofetch'
fi
